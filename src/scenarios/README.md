# 시나리오 테스트 저장 폴더

콘솔 [시나리오 테스트]에서 만든 시나리오가 JSON 파일 하나씩으로 저장됩니다 (`<id>.json`).
`.env` 에 `APP_WORKSPACE=workspace` 가 있으면 `workspace/scenarios/` 에 저장됩니다.

- v1.11.6 부터 서버에 저장됩니다 — 예전에는 브라우저 localStorage 에만 있어 팀원과 공유되지 않았고 백업에도 빠졌습니다.
- 처음 열 때 브라우저에 남아 있던 시나리오는 자동으로 서버로 옮겨집니다.
- [백업/복원] zip 에 함께 담깁니다.
