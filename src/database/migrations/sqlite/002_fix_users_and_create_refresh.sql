-- =====================================================================
-- SQLite 에서는 001_init_auth.sql 이 이미 올바른 스키마를 생성하므로
-- 이 파일은 no-op 입니다 (MariaDB 에서는 legacy password 컬럼 제거 + index fix 역할).
--
-- 파일명은 유지해야 schema_migrations 테이블에 기록되어 "스키마 버전 일관성"이
-- 다른 어댑터 DB 와 동일하게 관리됩니다.
-- =====================================================================
SELECT 1 AS _noop;
