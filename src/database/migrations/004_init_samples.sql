-- @migration-options: non-blocking
--   샘플 데이터 파일입니다. 이 파일이 실패해도 뒤따르는 마이그레이션(users.name,
--   화면 프로젝트, admin 비밀번호 정책 등)은 계속 실행됩니다 — 샘플 하나 때문에
--   로그인/콘솔이 깨지지 않도록. 실패한 파일은 기록되지 않으므로 원인을 고치고
--   서버를 다시 시작하면 자동으로 재시도합니다.
-- ============================================================
-- v1.2.0: 샘플 컨트롤러용 테이블 (MariaDB)
--   Person / Student / Weight / BloodPressure / BloodSugar / SecureMember
--   이전 SQL 은 aidot_express.* / weight_db.* 처럼 스키마명을 하드코딩해 DB 이름이 다르거나
--   SQLite 이면 전부 실패했다. 이제 현재 DB 안의 평범한 테이블을 사용한다.
-- ============================================================

-- ★ v1.10.41 — 내용은 007/008/009 로 나뉘었습니다.
--   한 파일에 9개가 뭉쳐 있어 갈래별로 켜고 끌 수 없었습니다.
--   이 파일은 **이미 적용된 기록**이 있는 환경을 위해 자리만 지킵니다.
