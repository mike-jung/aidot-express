-- ============================================================
-- (v1.2.0) 기본 관리자 시드는 SQL 에서 제거되었습니다.
--   초기 관리자 계정은 부팅 시 JS 부트스트랩(lib/admin/service/AdminBootstrap.js)이 생성합니다.
--     - development : admin / admin1234  (콘솔 접속 후 [사용자 관리 → 내 비밀번호 변경] 으로 교체)
--     - production  : ADMIN_INITIAL_PASSWORD 환경변수 값, 없으면 1회용 랜덤 비밀번호를 콘솔 로그에 출력
--   이 파일은 schema_migrations 기록 호환을 위해 이름만 유지합니다 (실행 문장 없음).
-- ============================================================
